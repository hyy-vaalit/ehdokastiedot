<h1>Renkaiden väliset arvonnat</h1>

<p><%= link_to 'Takaisin kaikkiin arvontoihin', draws_path %></p>

<% @coalition_draws.each do |draw| -%>
  <div class="draw" id="draw_<%= draw.id %>">
    <ul>
      <% draw.coalition_drawings.rank(:position_in_draw).each_with_index do |drawing, i| -%>
        <li><%= get_draw_candidate_from_coalition(drawing) %>: <%= i+1 if draw.drawed %></li>
      <% end -%>
    </ul>
    <p><%= link_to 'Siirry arvontaan', edit_draws_coalition_path(draw.id) %></p>
  </div>
<% end -%>
