<%= raw(JSONBuilder::Compiler.generate do
  name "Äänestysalueen mukaan"
  year Time.now.year
  created_at Time.now.localtime

  children do
    voted do
      men     Voter.voted.men.count
      women   Voter.voted.women.count
      unknown Voter.unknown_gender_voted_count
    end

    total do
      men     Voter.men.count
      women   Voter.women.count
      unknown Voter.unknown_gender_count
    end

  end

end) %>
