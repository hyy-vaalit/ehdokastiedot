<%# Do not indent this file: it will mess up the CSV format. -%>
<% if @alliances.any? -%>
<%= ElectoralAllianceExport.new(@alliances).to_csv(encoding: @encoding).html_safe -%>
<% else %>
Vaalijärjestelmään ei ole vielä syötetty yhtään vaaliliittoa.
<% end %>
